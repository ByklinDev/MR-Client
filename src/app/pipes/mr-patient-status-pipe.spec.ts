import { MrPatientStatusPipe } from './mr-patient-status-pipe';

describe('MrPatientStatusPipePipe', () => {
  it('create an instance', () => {
    const pipe = new MrPatientStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
