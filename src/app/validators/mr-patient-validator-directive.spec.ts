import { MrPatientValidatorDirective } from './mr-patient-validator-directive';

describe('MrPatientValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new MrPatientValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
